function array(arr,num) {
    let arrResult = [];
    for(let i = 0; i < arr.length; i+= num) {
      
        arrResult.push(arr[i]);
    
    }
return arrResult;

}

array(['5', '31', '20'])
array(['dsa','asd','test','tset'],2)